apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.frontend.container.name }}-service
  namespace: {{ .Release.Namespace }}
spec:
  type: {{ .Values.frontend.service.type }}
  selector:
    app: {{ .Values.frontend.container.name }}
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: {{ .Values.frontend.container.port }}
      nodePort: {{ .Values.frontend.service.nodePort }}
    - name: ssh
      port: {{ .Values.frontend.service.sshPort }}
      targetPort: {{ .Values.frontend.service.sshPort }}
      nodePort: {{ .Values.frontend.service.sshNodePort }}
