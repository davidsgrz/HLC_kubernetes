apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pokeapi-nest.fullname" . }}-test-connection"
  labels:
    {{- include "pokeapi-nest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pokeapi-nest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
